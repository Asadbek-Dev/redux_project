const ADD_TODO = 'ADD_TODO'

export const add_todo = (message) => ({
    type: ADD_TODO,
    message,
    id: Math.random(),
});

const DELETE_TODO = 'DELETE_TODO'

export const add_todo = (message) => ({
    type: ADD_TODO,
    message,
    id
});

